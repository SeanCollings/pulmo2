const BUILD_VERSION = '1.0.0';
const ANDROID_VERSION = 2;
const IOS_VERSION = '1.0.0';
const RELEASE_ID = '#{RELEASE_ID}#';

module.exports = {
  BUILD_VERSION,
  ANDROID_VERSION,
  IOS_VERSION,
  RELEASE_ID,
};
